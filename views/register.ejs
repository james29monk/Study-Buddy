<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="/register.css">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register Page</title>
</head>
<body>
    <div class="content">
        <div class="imgContainer">
        <img src="" alt="logo"></div>
<form class="container" action="/register" method="post">
<div class="form">
    <legend> Create account </legend>
    <% if (typeof successMessage !=='undefined' ) { %>
        <p style="color: green;">
            <%= successMessage %>
        </p>
        <% } %>
     <% if (typeof failedMessage !=='undefined' ) { %>
            <p style="color: rgb(247, 24, 8);">
                <%= failedMessage %>
            </p>
            <% } %>
    <% { %>
        <div class="name">
        First name <input required type="text" name="firstName" maxlength="26"/><br><br></div>
        <div class="name">
        Last name <input required type="text" name="lastName" maxlength="26"/><br><br></div>
        <div class="email">
        Email <input required type="email" name="email" value="" /><br><br></div>
        <div class="password">
        Password <input required type="password" placeholder="at least 8 characters" minlength="8" maxlength="50" name="password" value="" /><br><br><p class="passConstraint">Passwords must be at least 8 characters.</p></div>
        <div class="password2">
        Re-enter password <input required type="password" name = "repassword"  minlength="8" value = "" /> <br><br></div>
    <% } %>

    <button onclick="">Create your registration account</button>
    <div class="line"></div>
    <p class="account">Already have an account? <a href="http://localhost:3000/login">Sign in</a></p>
</div>
</form>
<div class="line2"></div>  
 
</div>
</body>
</html>